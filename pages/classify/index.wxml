<view class="container">
  <view class="classify section">
    <view class="classify-item" wx:for="{{classify}}" wx:key="item">
      <image mode="aspectFill" src="{{item.cover}}" bindtap="navigateToWorksList" data-item="{{item}}"></image>
    </view>
  </view>
</view>